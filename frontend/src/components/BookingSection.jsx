import { tours } from '../data/siteData'; import { site } from '../data/siteConfig'; import { useLanguage } from '../context/LanguageContext';
export default function BookingSection(){const {tx}=useLanguage();return <section className="booking"><div className="container booking-inner"><div><div className="eyebrow">{tx('READY WHEN YOU ARE')}</div><h2>{tx('Plan Your Dubai Experience')}</h2><p>{tx('Send your dates and preferred tour. Our team can confirm availability and tailor the itinerary.')}</p><a className="btn btn-primary" href={site.whatsapp}>{tx('WhatsApp — Fast Response')}</a></div><form onSubmit={e=>{e.preventDefault();window.location.href=site.whatsapp}}><input placeholder={tx('Full Name')} required/><input type="email" placeholder={tx('Email (optional)')}/><input placeholder={tx('Phone / WhatsApp')} required/><select defaultValue=""><option value="" disabled>{tx('Choose a tour')}</option>{tours.map(t=><option key={t.slug}>{tx(t.title)}</option>)}</select><input type="date"/><textarea placeholder={tx('Message (optional)')} rows="4"/><button className="btn btn-primary">{tx('Request Booking')}</button></form></div></section>}
