import React from 'react';
import { Link } from 'react-router-dom'; import SEO from '../components/SEO'; import Section from '../components/Section'; import { posts } from '../data/siteData'; import { useLanguage } from '../context/LanguageContext';
export default function Blog(){const {tx}=useLanguage();return <><SEO title="Dubai Travel Guides" description="Dubai desert safari, city sightseeing and travel planning guides." path="/blog" breadcrumbs={[{name:'Home',path:'/'},{name:'Travel Guides',path:'/blog'}]}/><div className="inner-hero"><div className="container"><div className="eyebrow">{tx('TRAVEL GUIDES')}</div><h1>{tx('Plan Dubai With Confidence')}</h1><p>{tx('Practical notes for desert adventures, city sightseeing and first-time visitors.')}</p></div></div><Section title={tx('Latest Guides')}><div className="post-grid">{posts.map(p=><article className="post-card" key={p.slug}><img src={p.image} alt={`${tx(p.title)} — Zuhd Tours`} loading="lazy"/><div><span>{tx(p.category)}</span><h3>{tx(p.title)}</h3><p>{tx(p.excerpt)}</p><Link to={`/blog/${p.slug}`}>{tx('Read guide')} →</Link></div></article>)}</div></Section></>}
