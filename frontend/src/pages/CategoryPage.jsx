import SEO from '../components/SEO'; import Section from '../components/Section'; import TourGrid from '../components/TourGrid'; import BookingSection from '../components/BookingSection'; import { useLanguage } from '../context/LanguageContext';
export default function CategoryPage({type,title,description,path}){const {tx}=useLanguage();const isSafari=type==='Desert Safari';return <><SEO title={title} description={description} path={path} breadcrumbs={[{name:'Home',path:'/'},{name:title,path}]}/><div className="inner-hero"><div className="container"><div className="eyebrow">{tx(isSafari?'DESERT EXPERIENCES':'CITY EXPERIENCES')}</div><h1>{tx(title)}</h1><p>{tx(description)}</p></div></div><Section title={tx(isSafari?'Choose Your Safari':'Choose Your City Experience')}><TourGrid category={type}/></Section><BookingSection/></>}
